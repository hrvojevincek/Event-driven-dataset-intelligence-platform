{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eventforge.dev/schemas/project.submitted.schema.json",
  "title": "ProjectSubmittedPayload",
  "description": "Payload for eventforge.project.submitted — starts the dataset pipeline.",
  "type": "object",
  "required": ["name", "schema_json"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable project name; maps to jobs.name."
    },
    "schema_json": {
      "type": "object",
      "description": "Annotation output schema (JSON Schema or field map)."
    },
    "schema_template": {
      "type": "string",
      "maxLength": 64,
      "description": "Optional preset template id, e.g. support_call or document_classification."
    },
    "domain": {
      "type": "string",
      "maxLength": 32,
      "default": "documents",
      "description": "Content domain for the project; maps to jobs.domain."
    },
    "asset_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Optional number of uploaded assets known at submit time."
    }
  },
  "additionalProperties": false
}
